import React from 'react'
import { useSelector } from 'react-redux';
import { useNavigate, useParams } from 'react-router-dom'

export default function LawyersInfo(): JSX.Element {
    // const {id} = useParams;
    // const navigation = useNavigate();
    // const {lawyers} = useSelector((store: RootState) => store.lawyerState);
    // const law = lawyers.find((lawyer) => lawyer.id === Number(lawyerId?.[1]));
  return (
    <div>L</div>
  )
}
